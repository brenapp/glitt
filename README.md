# GiTTY

GiTTY is a small editor for standard git workflows that makes it easier to use Git from the terminal. We currently provide the following editors, and allow you to default to a configured editor

```
[core]
    editor = "./target/debug/glitt --fallback vim $@"
```
